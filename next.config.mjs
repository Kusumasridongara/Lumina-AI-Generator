/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['cdn-icons-png.flaticon.com']
    },
    devIndicators: {
        buildActivity: false,
    },
};

export default nextConfig;
